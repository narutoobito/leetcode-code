class Solution {
public:
    int minOperations(vector<int>& nums, int k) {
        int res=0;
        for(auto i:nums)res^=i;
        int count=0;

        res = res^k;

        while(res){
            if((res&1))count++;
            res=res>>1;
        }

        return count;
    }
};
